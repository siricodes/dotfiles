# Source other alias files
source $( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )/open
source $( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )/fzf
## PARAMETERS
# Basics
alias c="clear"
alias e=$EDITOR
# Speedtest
alias spd="speedtest --secure"
# Picom
alias picom="picom -b --experimental-backends"

## MINI-SCRIPTS <3
# Get qr code from screen
alias qrscreen="maim | zbarimg -q --raw -"
